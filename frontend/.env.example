# API base path: frontend calls this path, nginx (or Vite in dev) proxies to OpenFaaS.
# Default /api works for both dev (Vite proxy) and prod (nginx proxy). Only set if you use a different path.
# VITE_API_BASE=/api
